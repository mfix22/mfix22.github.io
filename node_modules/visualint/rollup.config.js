const babel = require('rollup-plugin-babel')

const umd = {
  input: 'index.js',
  output: [
    {
      name: 'VisuaLint',
      file: 'dist/visualint.umd.js',
      format: 'umd',
      sourcemap: true,
      exports: 'named',
      globals: {
        react: 'React'
      }
    }
  ],
  plugins: [
    babel({
      exclude: 'node_modules/**',
      presets: [
        ['env', { 'modules': false }],
        'react'
      ],
      runtimeHelpers: true,
      plugins: ['external-helpers', 'transform-runtime'],
      babelrc: false
    })
  ],
  external: mod => mod.startsWith('babel-runtime') || mod === 'react'
}

module.exports = umd
